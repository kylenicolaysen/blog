<div class="post-content">
    <ul>
    {{#each posts}}
        <li>
        <a href="/post/{{id}}"><h2>{{title}}</h2><p>_{{preview}}</p></a>
        </li>
    {{/each}}
    </ul>
</div>

<div class="footer">
    {{#if logged_in}}
        <a class="footer_utton" href="/add">_Add</a>
        <a class="footer_button" href="/logout">_Logout</a>
    {{else}}
        <a class="footer_button" href="/login">_Login</a>
    {{/if}}
</div>